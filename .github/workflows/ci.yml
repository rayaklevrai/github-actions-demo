name: CI
on: [push]
FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: List Files
        run: ls -la